sensor button: 9
actuator led: 12
actuator buzzer: 8

# Declaring states
on {
    led <= HIGH
    buzzer <= HIGH
    button is LOW => off
}

-> off {
    led <= LOW
    buzzer <= LOW
    button is HIGH => on
}

application exo

signal when on

